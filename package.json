{
  "name": "@zefiros/axioms",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zefiros-Software/axioms.git"
  },
  "version": "0.1.3",
  "description": "Build tools for Therefore",
  "files": [
    "dist/",
    "src/"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.dist.json && find dist -type f -regex '.*.js.map' -exec sed -i.bak 's|../src|./src|g' {} \\; && find dist -type f -regex '.*.bak' -exec rm {} \\; && cp -r package.json LICENSE CHANGELOG.md README.md src dist/",
    "test": "tsc && jest test",
    "fix": "yarn lint --fix",
    "lint": "tslint --project tsconfig.json",
    "release": "yarn standard-version -a",
    "release:dry": "yarn release --dry-run",
    "publish": "cd dist && yarn build && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@zefiros/npm-defaults": "^0.1.4"
  }
}
