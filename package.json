{
  "private": true,
  "name": "@zefiros/axioms",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zefiros-Software/axioms.git"
  },
  "version": "0.0.1",
  "description": "Build tools for Therefore",
  "files": ["dist/", "src/"],
  "scripts": {
    "build": "tsc -p tsconfig.dist.json",
    "test": "tsc && jest test",
    "fix": "yarn lint --fix",
    "lint": "tslint --project tsconfig.json",
    "release": "yarn standard-version -a",
    "release:dry": "yarn release --dry-run"
  },
  "dependencies": {},
  "devDependencies": {
    "@zefiros/npm-defaults": "^0.1.3"
  }
}
